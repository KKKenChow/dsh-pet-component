# 2026-09-16

> 用于同步上游 dsh-pet 的变化

## 上游基线

| 项 | 值 |
| --- | --- |
| 上游仓库 | `PC2005-cloud/dsh-pet`（本地 `source/dsh-pet`） |
| 记录基线（superproject gitlink） | `2136e5d`（`v0.2.7-17-g2136e5d`） |
| 评估范围 | `2136e5d..4c09729`，22 个提交（`v0.2.8` → `v0.2.10`，2026-09-11 → 2026-09-16） |
| 上游 HEAD | `4c09729`（`package.json` version `0.2.10`） |
| 本地工作区子模块 | 已检出于 `4c09729`（记录值仍是 `2136e5d`，属待裁决项） |

## 评估结论

**建议采纳 0 项 / 不采纳 21 项 / 待定 1 项。**

本仓库是纯渲染层，只消费配置协议（`animations` 池、`animationWeights`、`physics`、`pets[]`）。
整段 diff 里 `dsh-pet/src/shared/pickers.ts`、`constants.ts`（`HIT_BOX`）、`animations` 段与
workStatus 档位词表**零改动**；`814b0e4`（prettier）逐行核对为纯排版 —— `resolvePhysics`
默认值（1400 / 0.78 / 2.5 / true / 1.0 / false）与移植的 pickers 仍与上游一致。

唯一触及协议面的是 `70170a3` + `6dcf087`：`dsh-pet/assets/config.jsonc` 全文件只有 1 个 hunk
（`@@ -13,0 +14,45 @@`），新增 `whisperImageEnabled` / `chatImageEnabled` / `memes`（27 条
名称→描述）；同提交新增 `assets/memes/*.png` 27 张；`src/shared/types.ts` 的 `ClientConfig`
+4 行。该协议已随 v0.2.0 的碎碎念配图落地（实现见 `docs/spec/spec-2.md`）。

其余提交全部是 dsh-pet 宿主应用：

| 提交 | 内容 | 不采纳原因 |
| --- | --- | --- |
| `6f96508` `f49f14a` | host `/thumb` 路由 petId 防穿越（Windows 反斜杠越根）+ 非法 petId 400 | 宿主路由，本仓库不实现 |
| `74bfc59` `1670d2f` `728dfcd` `558643f` `4c09729` | Electron helper 熔断 / 窗口显示 / 点击穿透主进程通道 | 宿主专属（spec §1.2） |
| `6180c8a` | notify 传输改 host 转发，适配 DSH 0.1.5 移除 `api.events.mux/host` | 宿主通知链路 |
| `6199d9d` | 余额气泡文案行（`unavailableRows`）+ `decideBalanceNotice` | 只改气泡文案；档位数学 `balanceEventIndex` / `balancePercent` 未动，`dshBalanceTier` 仍一致 |
| `b715fa3` `22db345` `b8cb017` | 设置页 + 条目级 `physics` / `workStatusTexts` 回灌 | 宿主设置页；条目级字段基线 `2136e5d` 就已存在 |
| `b3eb1c0` `27011f0` `e67cd5f` | 碎碎念 / 对话配图（`shared/whisper.ts`、`client/bubble.ts`、`host/memes.ts`） | 宿主气泡能力；协议面（`memes`）已由 v0.2.0 覆盖 |
| `22d664f` | hevc-alpha 流水线改固定 tag Release | CI |
| `b400159` `814b0e4` | `v0.2.9` 发布收尾与 prettier | 发布琐事 / 纯排版 |

## 待裁决

1. **gitlink 是否推进到 `4c09729`**（`source/dsh-pet`）：本地工作区已检出于该提交，但
   superproject 记录的仍是 `2136e5d`，所以 `git status` 显示 ` M source/dsh-pet`。
   推进属基线簿记、零代码影响，**等用户裁决后再动**。
2. **配图类型字段**（`whisperImageEnabled` / `chatImageEnabled` / `memes` 补进
   `DshPetConfig`）：已随 v0.2.0 配图落地，此项自动成立。
3. **本轮记录的落点**：本文件即用户批准的方案 F（新建 `docs/sync/2026-09-16.md`），
   `docs/sync/2026-09-11.md` 保持空 stub。

## 提交约定提醒

提交时**只显式 stage 本次改动的文件**，不要 `git add -A`：工作区里 `source/dsh-pet`、
`source/dsh-dafeiyu`、`source/deepseek-harness-desktop` 三个子模块的 HEAD 都超前于
superproject 的记录值，一并 stage 会把 gitlink 变动混进来。
